[tool.poetry]
name = "cristales-backend"
version = "0.1.0"
description = "Backend scaffold"

[tool.poetry.dependencies]
python = "^3.11"
fastapi = "^0.95"
uvicorn = "^0.22"

[tool.poetry.dev-dependencies]
pytest = "^7.0"
